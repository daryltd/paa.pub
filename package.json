{
  "name": "paa-pub",
  "version": "0.1.0",
  "private": true,
  "description": "Single-user Solid + ActivityPub server on Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.67.0"
  },
  "dependencies": {
    "mustache": "^4.2.0"
  }
}
